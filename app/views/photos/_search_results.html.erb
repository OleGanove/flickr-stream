<div class="search-results">
  <% if @user %>
    <h1 class="title"><%= @user.username %>'s photos</h1>
    <% @user_photos_urls.each do |url| %>
      <tr>
        <td><%= link_to( image_tag(url), url, :class => "picture" ) %></td>
	    
      </tr>
    <% end %>
  <% end %>
</div>

